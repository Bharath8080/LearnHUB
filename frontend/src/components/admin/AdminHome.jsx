import React from 'react';
import { Container } from 'react-bootstrap';

const AdminHome = () => {
    return (
        <Container>
            <h2>Admin Dashboard</h2>
            <p>Admin features (User management, etc.) would go here.</p>
        </Container>
    );
};

export default AdminHome;
